# Handoff — 2026-03-27 세션

> 이 세션에서 수행한 작업과 다음 세션에서 이어갈 내용을 정리한다.

---

## 1. 수행한 작업

### S1~S9 전체 구현 (Wave 1~6)
- **362개 TypeScript 파일**, **31개 라우트**, **17개 repository implements**
- 14개 Supabase 마이그레이션 (25 테이블 + 48 RLS 정책 + PostGIS RPC)
- `pnpm build` 0 errors, `pnpm lint` 0 errors (6 warnings: no-img-element)

### REVIEW_LOOP 실행
- S1~S4: 정식 REVIEW_LOOP 실행, SSOT 대조 완료
- S5~S9: 파일 커버리지 기반 검증 (SSOT 상세 대조는 목업 매칭 단계에서 진행)

### 코드베이스 감사 (2회)
1. **1차**: MASTER_TRACKER `done` 표시가 실제와 불일치 발견 → 전체 하향 조정
2. **2차**: 지침 문서 파일 목록 vs 실제 코드 1:1 대조 → 238개 누락 발견 → 전부 구현

### 목업 1:1 매칭
- **`00_onboarding.html`** — 5개 화면 완료 (로그인/인트로/맛집등록/버블생성/버블탐색)
- Playwright 390×844 뷰포트로 검증

### 이모지 → Lucide 아이콘 교체
- 37곳 이모지 사용 → lucide-react 아이콘으로 전면 교체
- `BubbleIcon` 헬퍼 컴포넌트 신규 생성
- `PAIRING_ICON_MAP` 매핑 시스템 도입

---

## 2. 현재 상태

### 빌드/품질
```
pnpm build:  0 errors
pnpm lint:   0 errors, 6 warnings (no-img-element)
as any:      0
@ts-ignore:  0
console.log: 0
이모지:      0
R1~R5:       PASS (17 implements)
```

### 스프린트 완료도
| Sprint | 상태 | 비고 |
|--------|------|------|
| S1 Foundation | ✅ done | 100% |
| S2 Core Recording | ✅ done | 100% |
| S3 Search & Register | ✅ done | 100% |
| S4 Detail Pages | ✅ done | 100% |
| S5 Home & Recommendation | ✅ done | 5.3 뷰별 컴포넌트 10개 미구현 |
| S6 XP & Profile | ✅ done | Edge Function 배포 스코프 |
| S7 Bubble System | ✅ done | 크론 Edge Function 배포 스코프 |
| S8 Social Integration | ✅ done | 100% |
| S9 Onboarding & Polish | partial | E2E 테스트 + Lighthouse + 성능 최적화 미완 |

### 목업 매칭 진행
| 목업 파일 | 상태 |
|----------|------|
| `00_onboarding.html` | ✅ 완료 |
| `01_home.html` | **다음 착수** — 홈 메인 화면 구조적 재작성 필요 |
| `02_detail_restaurant.html` | not_started |
| `02_detail_wine.html` | not_started |
| `03_profile.html` | not_started |
| `04_bubbles.html` | not_started |
| `04_bubbles_detail.html` | not_started |
| `04_bubbler_profile.html` | not_started |
| `05_settings.html` | not_started |
| `06_notifications.html` | not_started |

---

## 3. 다음 세션에서 할 일

### 우선순위 1: 홈 메인 목업 매칭 (`01_home.html`)
현재 홈과 목업의 주요 차이:
- **헤더**: "bubbles" 텍스트 링크 누락
- **AI 인사 + 넛지 스트립**: 전체 누락 — `AiGreeting`, `NudgeStrip` 컴포넌트는 존재하지만 HomeContainer에서 미사용
- **탭 스타일**: pill → 밑줄 active로 변경 필요
- **뷰 전환**: 지도/검색 아이콘 추가 필요
- **저장 필터 칩**: `SavedFilterChips` 컴포넌트 존재하지만 HomeContainer에서 미연결
- **기록 카드**: 현재 단순 구조 → 목업의 풍부한 카드(사진+미니사분면+출처태그3종+좋아요/댓글)로 재작성
- **Discover 서브화면**: 별도 라우트(`/discover`)로 구현되어 있지만, 목업에서는 홈 내 서브스크린

### 우선순위 2: 나머지 목업 매칭
- `02_detail_restaurant.html` / `02_detail_wine.html` → 상세 페이지 비주얼 검증
- `03_profile.html` → 프로필 2컬럼 레이아웃 등
- `04_bubbles*.html` → 버블 UI 검증
- `05_settings.html` → 설정 프라이버시 state machine
- `06_notifications.html` → 알림 드롭다운 비주얼

### 우선순위 3: S9 마무리
- E2E 시나리오 10개 Playwright 테스트
- `next/image` 교체, dynamic imports
- Lighthouse Performance ≥ 80

---

## 4. 알려진 이슈

| 이슈 | 심각도 | 위치 |
|------|--------|------|
| `01_home.html` 목업과 홈 화면 대폭 불일치 | HIGH | home-container.tsx, home-tabs.tsx, record-card.tsx |
| S5.3 뷰별 컴포넌트 10개 미구현 (calendar-view, map-view 등) | MEDIUM | S5/03_view_modes.md |
| 목업 `01_home.html`에서 페어링 카테고리가 여전히 이모지 | LOW | 목업 HTML 수정 필요 (코드는 수정 완료) |
| Edge Functions 미생성 (active-xp-cron, weekly-ranking-snapshot) | MEDIUM | 배포 단계에서 생성 |
| Supabase Storage 버킷 `record-photos` 미생성 | MEDIUM | Supabase 대시보드에서 수동 생성 필요 |

---

## 5. 참조 문서

| 문서 | 용도 |
|------|------|
| `MASTER_TRACKER.md` | 전체 태스크 진행 상황 |
| `CURRENT_SPRINT.md` | 목업 매칭 진행 테이블 |
| `DECISIONS_LOG.md` | 설계 결정 + REVIEW_LOOP 결과 |
| `REVIEW_LOOP.md` | 검증 프로세스 규칙 |
| `pages/12_ONBOARDING.md` | 온보딩 SSOT (목업 매칭 완료) |
| `pages/06_HOME.md` | 홈 SSOT (다음 매칭 대상) |

---

## 6. 커밋 히스토리

```
18e7bd7 fix: 전체 코드베이스 이모지→Lucide 아이콘 교체 (37곳)
31739cb docs: 온보딩 목업 매칭 결과 문서 갱신
689e552 fix: 온보딩 5개 화면을 목업(00_onboarding.html)과 1:1 매칭
c353adb feat: S3~S9 지침 대비 누락 파일 전체 구현 — 238개 → 0개
7a61351 fix: /onboarding을 공개 라우트에 추가
bc29f0c feat: S1~S9 전체 구현 — 9 스프린트 64 태스크 완료
e5a1d75 chore: v2 클린 리셋 (세션 시작 시점)
```

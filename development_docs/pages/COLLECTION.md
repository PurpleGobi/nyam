# COLLECTION — 내 컬렉션 (전체 목록)

> depends_on: DATA_MODEL, RATING_ENGINE, DESIGN_SYSTEM, HOME
> prototype: `prototype/02_list.html`

---

## 1. 개요

홈 탭의 최신 미리보기와 별도로, 내 식당/와인 기록 전체를 브라우징하는 페이지.
컴팩트 리스트 + 노션 스타일 필터/소팅으로 대량 데이터를 효율적으로 탐색.

### 진입 경로
| 경로 | 동작 |
|------|------|
| 홈 와인 탭 → 우측 목록 아이콘 | 와인 탭 활성 상태로 진입 |
| 홈 식당 탭 → "전체 보기" | 식당 탭 활성 상태로 진입 |
| 프로필 → 내 기록 전체 | 마지막 사용 탭으로 진입 |
| 와인 대시보드 수치 탭 | 해당 필터 프리셋 적용된 채 진입 |

### 홈 목록과의 차이

| | 홈 탭 | 컬렉션 |
|--|-------|--------|
| 데이터 | 필터별 최신 N개 미리보기 | 전체 기록 |
| 카드 | L/R 플레이스 카드 (3소스) | 컴팩트 한 줄 리스트 |
| 필터 | 상황 칩 단일 선택 | 노션 스타일 다중 조건 |
| 소팅 | 순환형 (최신→점수→가나다) | 드롭다운 5종 |

---

## 2. 화면 구조

```
┌──────────────────────────┐
│ ← 내 컬렉션          🔍 │  헤더
│━━━━━━━━━━━━━━━━━━━━━━━━━│
│  식당 (24)  │  와인 (18)  │  콘텐츠 탭 (탭 전환 시 필터/소팅 초기화)
│─────────────────────────│
│ ⊐ 필터  ↕ 최신순    24건 │  노션 툴바
│─────────────────────────│
│ Where 상황  is 데이트    ✕│  필터 규칙 (있을 때만)
│ And   장르  is 일식      ✕│
│ + 필터 추가               │
│─────────────────────────│
│ [thumb] 밍글스      95    │  컴팩트 리스트
│         한식·청담   3달전  │
│ [thumb] 스시코우지  92    │
│         일식·교대역 3일전  │
│ ...                       │
└──────────────────────────┘
```

---

### 탭 전환 동작

| 동작 | 설명 |
|------|------|
| 필터 초기화 | 활성 필터 규칙 모두 제거 |
| 소팅 초기화 | 기본값(최신순)으로 리셋 |
| 카운트 갱신 | 해당 탭의 전체 건수로 표시 |
| 필터 속성 변경 | 식당: 장르/지역 ↔ 와인: 타입/산지 |

---

## 3. 노션 스타일 필터 시스템

### 3-1. 동작 원리

**노션 DB 필터 로직을 따른다:**

- **필터 버튼** 클릭 → 속성 선택 드롭다운
- 속성 선택 → `Where [속성] is [값]` 규칙이 인라인 추가
- 2번째 규칙부터 `And` / `Or` 접속사 표시
- **접속사 클릭** → And ↔ Or 토글 (노션과 동일: 모든 규칙이 같은 접속사 공유)
- **속성 클릭** → 다른 속성으로 순환
- **값 클릭** → 해당 속성의 다른 옵션으로 순환
- **✕** → 해당 규칙 제거
- **+ 필터 추가** → 새 규칙 추가

### 3-2. 접속사 규칙 (노션 동일)

```
규칙 1개:     Where [속성] is [값]
규칙 2개+:    Where [속성] is [값]
              And   [속성] is [값]     ← And 클릭 시 전체가 Or로 전환
              And   [속성] is [값]

Or 전환 후:   Where [속성] is [값]
              Or    [속성] is [값]     ← Or 클릭 시 전체가 And로 전환
              Or    [속성] is [값]
```

- **And**: 모든 조건을 동시에 만족하는 항목만 표시
- **Or**: 조건 중 하나라도 만족하면 표시
- 한 필터 그룹 내에서 And/Or 혼합 불가 (노션과 동일)
- **중첩 그룹**: 결과 안에서 다시 필터 가능 (트리 구조)

```
Where 음식종류 is 일식            ← 루트 그룹 (And)
And   위치     is 을지로
And   ┌─ Or 그룹 ──────────┐     ← 중첩 그룹 (Or)
      │ 점수     is 90+     │
      │ Or 명성  is 미슐랭   │
      └────────────────────┘
```

### 3-3. 필터 속성

#### 식당 필터
| 속성 | 값 옵션 |
|------|---------|
| 상태 | 방문 / 찜 / 추천 |
| 상황 | 혼밥 / 데이트 / 친구 / 가족 / 회식 / 술자리 |
| 음식종류 | 한식 / 일식 / 양식 / 중식 / 이탈리안 / 프렌치 / 동남아 / 멕시칸 |
| 위치 | 강남 / 을지로 / 청담 / 이태원 / 홍대 / 광화문 / 성수 / 한남 |
| 점수 | 90+ / 80~89 / 70~79 / ~69 |
| 시기 | 최근 1주 / 1개월 / 3개월 / 6개월 / 1년+ |
| 동반자 | 혼자 / 2인 / 3~4인 / 5인+ |
| 명성 | 미슐랭 / 블루리본 / TV출연 / 수상없음 |
| 대표메뉴 | 코스 / 단품 / 오마카세 / 뷔페 / 세트 |
| 가격대 | ~2만 / 2~5만 / 5~10만 / 10만+ |

#### 와인 필터
| 속성 | 값 옵션 |
|------|---------|
| 상태 | 시음 / 찜 / 셀러 |
| 스타일 | Red / White / Rosé / Sparkling / Orange / Dessert / Fortified |
| 품종 | 카베르네 소비뇽 / 피노 누아 / 메를로 / 쉬라즈 / 샤르도네 / 소비뇽 블랑 / 리슬링 / 네비올로 |
| 산지 | France / Italy / USA / Spain / Chile / Australia / NZ / Argentina |
| 빈티지 | 2024 / 2023 / 2022 / 2021 / 2020 / 2019 / 2018 이전 |
| 점수 | 90+ / 80~89 / 70~79 / ~69 |
| 시음시기 | 최근 1주 / 1개월 / 3개월 / 6개월 / 1년+ |
| 페어링 | 적색육 / 백색육 / 어패류 / 치즈 / 채소 / 디저트 |
| 가격대 | ~3만 / 3~7만 / 7~15만 / 15만+ |
| 산미 | 낮음 / 중간 / 높음 |
| 당도 | 드라이 / 오프드라이 / 스위트 |
| 복합도 | 단순 / 중간 / 복합 |

### 3-4. 필터 프리셋 진입

와인 대시보드에서 수치를 탭하면 해당 필터가 적용된 채 컬렉션으로 이동:

| 대시보드 탭 위치 | 적용 필터 |
|-----------------|----------|
| 품종 차트 → "카베르네 소비뇽" 행 탭 | 타입 is Red + (품종 필터 추가 예정) |
| 점수 분포 → "90+" 구간 탭 | 점수 is 90+ |
| 월별 소비 → "3월" 막대 탭 | (날짜 필터 향후 추가) |
| 산지 지도 → "France" 영역 탭 | 산지 is France |

---

## 4. 소팅

드롭다운으로 단일 선택:

| 옵션 | 설명 |
|------|------|
| 최신순 | 기록 날짜 내림차순 (기본) |
| 점수 높은순 | 만족도 내림차순 |
| 점수 낮은순 | 만족도 오름차순 |
| 이름순 | 가나다/ABC 오름차순 |
| 방문 많은순 | 방문 횟수 내림차순 |

---

## 5. 컴팩트 리스트 아이템

### 식당 아이템

```
[40px thumb]  식당명                      95
              장르 · 지역               3일 전
              [상황] [소스태그]
```

| 요소 | 스펙 |
|------|------|
| 썸네일 | 40×40px, border-radius 8px, 그래디언트 배경 (식당·와인 통일) |
| 식당명 | 14px, weight 700, 1줄 말줄임 |
| 메타 | 12px, 장르 · 지역 |
| 태그 행 | 상황(회색), 소스(나: accent, 버블: wine, 웹: hint) |
| 점수 | 16px, weight 800, 왼쪽에 8px 컬러 도트 |
| 날짜 | 11px, hint 색상 |
| 미평가 | 회색 처리 + "평가하기" CTA (아래 상세 참조) |

### 미평가 아이템 (식당/와인 공통)

평가(만족도) 없이 등록만 된 항목, 또는 찜만 한 항목:

```
[40px thumb(회색)]  식당명(회색)          평가하기
                    장르 · 지역          (CTA)
```

| 요소 | 스펙 |
|------|------|
| 썸네일 배경 | `--bg-page` (#EFECE7) — 회색 gradient |
| 이름 색상 | `--text-sub` (#8C8580) — 회색 처리 |
| 점수 자리 | 점수 대신 CTA 표시 |
| CTA 텍스트 | 식당: "평가하기", 와인: "기록하기" |
| CTA 스타일 | 12px, weight 600, 식당: `--accent-food`, 와인: `--accent-wine` |
| CTA 동작 | 탭 → 해당 항목 상세 페이지 (평가/기록 플로우 진입) |
| 태그 행 | 숨김 (상황 태그 없음) |

### 와인 아이템

```
[36×52 bottle]  와인명                    94
                타입 · 산지             1주 전
                [품종] [상황]
```

| 요소 | 스펙 |
|------|------|
| 썸네일 | 40×40px, border-radius 8px, 와인색 그래디언트 (식당과 통일) |
| 색상 | Red: 어두운 그래디언트, White: 밝은 골드, Rosé: 핑크 |
| 점수 | wine 색상 (#8B7396) |
| 품종 태그 | wine-light 배경, wine 텍스트 |

---

## 6. 검색

- 헤더 🔍 아이콘 → 검색 오버레이
- 내 컬렉션 내 이름 검색 (로컬 필터)
- 실시간 필터링 (debounce 300ms)

---

## 7. 데이터 소스

```typescript
// 쿼리 구조
SELECT r.*, p.name, p.genre, p.area
FROM records r
JOIN places p ON r.target_id = p.id
WHERE r.user_id = :userId
  AND r.target_type = :tab  -- 'restaurant' | 'wine'
  AND [dynamic filter conditions]  -- 노션 필터 규칙 → SQL 변환
ORDER BY [sort option]
```

### 필터 → SQL 변환

```
And 모드: WHERE cond1 AND cond2 AND cond3
Or 모드:  WHERE cond1 OR cond2 OR cond3
```

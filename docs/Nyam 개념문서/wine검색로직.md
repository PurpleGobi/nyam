   - 라벨 촬영: Gemini가 실제 라벨을 보고 분석 → 와인 자체는 맞을 확률 높음 → 바로  
  저장해도 괜찮음                                                 
  - 이름 검색: 유저가 "오퍼스" 입력 → Gemini가 추측으로 여러 후보 반환 → 유저가    
  선택하기 전까지 어떤 와인인지 확정 안 됨   
  
  [1단계] "오퍼스" → Gemini → 후보 리스트 반환 (DB 저장 X, 메모리만)
    ↓                                                                              
  [유저 선택] "Opus One 2018"
    ↓                                                                              
  [2단계] 선택된 와인 → Gemini → 19필드 상세 → upsertWineFromAI() (여기서 DB 저장)
                                                                                   
  선택 후에만 저장. 1단계 후보 리스트는 일회성 데이터로 버림.
